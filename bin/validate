#!/usr/bin/env python3
"""Check every file in data/ against the schema. Prints every problem it finds."""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from wednesdays.validate import main

if __name__ == "__main__":
    sys.exit(main())
